(** This module is responsible for taking command-line arguments from
    the client, parsing the command-line arguments, and then executing
    those arguments. It will allow the client to set up either a local
    or remote connection the game, allowing the client to play either on
    their on own or in a multi-player mode.*)
